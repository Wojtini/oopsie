#!/usr/bin/env ansible-playbook
- name: Deploy grafana
  hosts: grafana

  roles:
    - role: grafana
    - role: prometheus


- name: Deploy exporter nodes
  hosts: minecraft_servers

  roles:
    - role: node_exporter
